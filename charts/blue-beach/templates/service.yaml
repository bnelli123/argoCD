apiVersion: v1
kind: Service
metadata:
  name: {{ include "blue-beach.fullname" . }}
  labels:
    {{- include "blue-beach.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "blue-beach.selectorLabels" . | nindent 4 }}
