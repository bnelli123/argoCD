apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "blue-beach.fullname" . }}-test-connection"
  labels:
    {{- include "blue-beach.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "blue-beach.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
